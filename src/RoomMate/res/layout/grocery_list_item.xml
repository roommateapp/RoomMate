<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:background="#F3F3F3" >

    <TextView
        android:id="@+id/person_for_task"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@id/task_name"
        android:textDirection="rtl"
        android:textSize="18sp" />

    <EditText
        android:id="@+id/grocery_item"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/check_item"
        android:ems="10"
        android:inputType="text"
        android:textSize="18sp" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/delete_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="invisible"
        android:layout_toRightOf="@id/grocery_item"
        android:drawableTop="@android:drawable/ic_menu_close_clear_cancel" />

    <CheckBox
        android:id="@+id/check_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/grocery_item"
        android:layout_alignBottom="@+id/grocery_item"
        android:layout_alignParentLeft="true" />

</RelativeLayout>